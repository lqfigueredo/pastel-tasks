

# Adicionar botão de retorno à Landing Page na tela Auth

## Mudança

Adicionar um botão/link no topo esquerdo da página `Auth.tsx` para voltar à landing page (`/landing`). Usar um ícone de seta (ArrowLeft) com texto discreto, posicionado simetricamente ao ThemeToggle que já está no topo direito.

## Arquivo

| Arquivo | Mudança |
|---|---|
| `src/pages/Auth.tsx` | Adicionar link `← Voltar` no `absolute left-4 top-4` apontando para `/landing` |

